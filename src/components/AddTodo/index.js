import './index.css';

const AddTodo = () => {
  return (
    <section className="add-too">
      <input className="addTodo" type="text" placeholder="Create a new todo..." />
    </section>
  );
}

export default AddTodo;